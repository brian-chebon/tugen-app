import 'package:flutter_test/flutter_test.dart';

// Integration/smoke tests require Firebase and are run on device.
// Unit tests live in test/core/ and test/features/ — run with:
//   flutter test test/core/ test/features/
void main() {
  test('placeholder', () => expect(true, isTrue));
}
